# TakeTopDSH Team — 平台使用说明 / Platform Instructions

本说明涵盖 Windows / macOS / Linux 三平台。"拷贝即用"：整个 `TakeTopDshTeam` 目录拷贝到目标机器即可，**无需安装 .NET、无需编译**。

---

## 一、快速开始 / Quick Start

### Windows (x64)
1. 拷贝整个 `TakeTopDshTeam` 目录到目标电脑。
2. 双击 **`start.bat`**（或右键 → 以管理员身份运行，若端口被占用）。
3. 浏览器自动打开启动器界面 `http://127.0.0.1:46001`。
4. 在启动器界面点击 **"打开DSH"** 进入工作台（自动带 token，无需手动输入）。工作台为左右分栏：左栏为「文件管理 / 我的任务」两个 tab，右栏为 DSH 页面。

### macOS (Apple Silicon, arm64)
1. 拷贝整个 `TakeTopDshTeam` 目录到目标 Mac。
2. 打开终端（Terminal），进入目录：
   ```bash
   cd /path/to/TakeTopDshTeam
   chmod +x start.sh
   ./start.sh
   ```
3. 首次运行会自动解压 Node 并安装 `@deepseek-ai/dsh`（**需联网一次**）。
4. 浏览器打开 `http://127.0.0.1:46001`，点 **"打开DSH"** 进入左右分栏工作台。

### Linux (x64 / arm64)
1. 拷贝整个 `TakeTopDshTeam` 目录到目标机器。
2. 打开终端，进入目录：
   ```bash
   cd /path/to/TakeTopDshTeam
   chmod +x start.sh
   ./start.sh
   ```
3. 首次运行自动解压 Node（**已内置，无需下载**）；仅 `@deepseek-ai/dsh` 的原生依赖需**联网安装一次**，之后离线可用。
4. 浏览器打开 `http://127.0.0.1:46001`，点 **"打开DSH"** 进入左右分栏工作台。

---

## 二、目录结构 / Folder Contents

```
TakeTopDshTeam/
├── start.bat              # Windows 启动脚本 / Windows launcher
├── start.sh               # macOS/Linux 启动脚本 / macOS & Linux launcher
├── dsh-launcher/
│   ├── win-x64/           # Windows self-contained 启动器 (exe)
│   ├── linux-x64/         # Linux x64 self-contained 启动器
│   ├── linux-arm64/       # Linux arm64 self-contained 启动器
│   └── osx-arm64/         # macOS Apple Silicon self-contained 启动器
├── node/
│   ├── (Windows node.exe + npm + dsh 依赖)   # Windows node/runtime
│   └── platforms/         # Linux/macOS Node 分发包 (tar)
├── config/                # 用户账号 (users.json) 与实例定义 (instances.json)
├── instances/              # 每个用户实例的 dsh 家目录 (instances/<user>/.dsh)
├── .dsh/                  # admin 默认 dsh 配置 / profiles / credentials
├── docs/                  # 工作经验数据等
├── appsettings.json       # DshWeb 配置 (URL/端口/默认语言/工作区)
└── wwwroot (内置)          # 管理页、工作台 (/work)、任务分配 (/tasks) 页面
```

- `dsh-launcher/<平台>/` 下的启动器是 **self-contained 单文件**（含 .NET 运行时），目标机无需装 .NET。

---

## 三、端口 / Ports

| 端口 Port | 用途 Purpose |
|-----------|-------------|
| `46001`   | 启动器界面（浏览器 UI）/ Launcher UI |
| `46000`   | admin 默认 DSH Web（需 token）/ admin default DSH (requires token) |
| `46002`   | 用户实例 jackzhong 的 DSH / user instance jackzhong |
| `46003`   | 用户实例 ericliu 的 DSH / user instance ericliu |
| `46004`   | 用户实例 sofiali 的 DSH / user instance sofiali |
| `46005`   | 用户实例 bookge 的 DSH / user instance bookge |

> ⚠️ **不要直接访问 `http://127.0.0.1:46000`**，会返回 404。DSH Web 必须带 `?token=` 才能访问。请通过启动器界面的 **"打开DSH"** 按钮进入（自动带 token）。

修改端口：编辑项目根 `appsettings.json` 的 `DshWeb/Url`，或在启动器界面"端口"输入框修改后点"保存"/"启动"。
Change the port by editing `DshWeb/Url` in `appsettings.json`, or via the launcher UI "Port" field + Save/Start.

**默认账号 / Default accounts：**

| 用户名 User | 默认密码 Default password | 说明 |
|--------|----------|------|
| admin | 12345678 | 管理员（启动器控制台 / 任务分配）/ admin (console & task assignment) |
| jackzhong | jack123 | 普通用户，实例 46002 |
| ericliu | eric123 | 普通用户，实例 46003 |
| sofiali | （由 admin 重置 / set by admin） | 实例 46004 |
| bookge | （由 admin 重置 / set by admin） | 实例 46005 |

> 提示：**admin 默认登录密码为 12345678**。其它账号密码可在控制台「实例管理 → Reset Password」设置/重置。

---

## 四、首次联网说明 / First-Run Network Notes

| 平台 Platform | Node 运行时 | @deepseek-ai/dsh 依赖 |
|--------------|------------|----------------------|
| Windows      | ✅ 已内置，离线 | ✅ 已内置，离线 |
| macOS/Linux  | ✅ 已内置（解压 tar，离线） | ⚠️ 首次需联网 `npm install` 一次，之后离线可用 |

- **Node 运行时**：三平台版本已在 `node/platforms/` 下载好，`start.sh` 直接解压，**无需联网**。
- **dsh 依赖**：含平台相关的原生模块（koffi/node-pty 等），Windows 二进制不能用于 Linux/macOS，因此 macOS/Linux **首次**须联网 `npm install` 一次（npm 会自动下载对应平台的原生模块），之后完全离线。
- **Windows**: Node and dsh both bundled, fully offline.
- **macOS/Linux**: Node bundled (offline). Only `@deepseek-ai/dsh`'s native deps need network on first run, then offline.

---

## 五、常见问题 / FAQ

**Q: 打开 `127.0.0.1:46000` 显示 404？**
A: 正常。请用启动器界面（46001）的"打开DSH"按钮，它会自动带 token。

**Q: 启动器界面打不开 / 端口被占用？**
A: 可能 `46001` 已被占用。可设置环境变量 `LAUNCHER_PORT` 改端口后用 `start.bat`/`start.sh` 启动；或关闭占用进程后重试。

**Q: macOS 提示"无法打开，因为无法验证开发者"？**
A: macOS 对未签名应用有限制。请右键 App → 打开；或在终端运行代码签名：
```bash
xattr -d com.apple.quarantine /path/to/TakeTopDshTeam/dsh-launcher/osx-arm64/TakeTopDshLauncher
```
大文件 single-file 被隔离时同样处理。

**Q: Linux 启动报 `chmod` / 权限错误？**
A: 确保执行过 `chmod +x start.sh`，且 `dsh-launcher/<平台>/TakeTopDshLauncher` 有执行权限（`start.sh` 已自动 `chmod +x`）。

**Q: 想改 dsh 默认模型 / API Key？**
A: 在 DSH Web 界面（带 token 打开后）→ **Settings → Models** 配置模型和 API Key；或编辑 `.dsh/` 下相关配置文件。

---

## 六、任务分配 / Task Assignment (multi-user)

- 启动器界面右上角有 **"任务分配"** 按钮，进入 `/tasks` 页面（管理员使用）。
- 左侧为成员列表（含 admin 及各用户），点击成员后右侧显示其任务列表：任务类型 / 任务名称 / 状态（处理中/完成/取消）/ 相关文件，支持添加、编辑、删除。
- 任务以 **XML** 存于该用户工作区 `<workspace>/TaskData/tasks-<id>.xml`；「相关文件」上传到 `<workspace>/TaskData/Doc`。
- 任务内容支持富文本（可粘贴/插入图片，图片同样存于 `TaskData/Doc`），双击内容里的图片可全屏预览。
- 用户在工作台左栏 **"我的任务"** tab 中可查看管理员分配给自己的任务（含任务名称链接查看详情、相关文件链接打开/下载）。
- The launcher UI has a **"任务分配 / Task Assignment"** button (admin) → `/tasks`. Tasks are stored as XML under `<workspace>/TaskData/tasks-<id>.xml`; related files go to `<workspace>/TaskData/Doc`. Users see assigned tasks in the workbench **"My Tasks"** tab.

---

## 七、测试环境 / Verified Environment

- 本包已测试：Windows x64 完整流程（start.bat → 启动器 → 自动拉起 DSH Web → 带 token 打开）。
- macOS / Linux 版本已在 Windows 上以 `dotnet publish -r <rid> --self-contained` 交叉发布（无原生 GUI 依赖），需在对应平台实际运行验证。Node 与 dsh 依赖首次运行自动准备。

---

## TL;DR (一句话)
拷贝 `TakeTopDshTeam` 到目标机 → Windows 双击 `start.bat` / macOS/Linux 运行 `./start.sh` → 浏览器访问 `http://127.0.0.1:46001` → 点 **"打开DSH"**。
