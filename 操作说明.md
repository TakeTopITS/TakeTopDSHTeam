# TakeTopDSH Team — 操作说明 / User Guide

本说明面向**日常使用者**，讲清：登录 → 打开工作台 → 文件管理 / 我的任务 → 管理员任务分配、实例管理、语言切换。

> ⚠️ **前提**：先运行启动脚本把 launcher 服务拉起来，浏览器才能访问 `http://127.0.0.1:46001`。
> - Windows：双击 **`start.bat`**
> - macOS / Linux：执行 **`./start.sh`**
> - 只要 launcher 还在后台运行，重新打开浏览器即可继续访问；若 launcher 已关闭（如重启电脑），需重新运行一次。

> 另一份《安装说明.md / INSTALL.md》讲如何安装启动；本文件讲**安装好之后怎么用**。

---

## 目录

- [一、整体界面长什么样](#一整体界面长什么样)
- [二、登录](#二登录)
- [三、启动器控制台（admin 视角）](#三启动器控制台admin-视角)
- [四、打开工作台 / 左右分栏](#四打开工作台--左右分栏)
- [五、文件管理（左栏）](#五文件管理左栏)
- [六、我的任务（左栏）](#六我的任务左栏)
- [七、任务分配页面 /tasks（admin）](#七任务分配页面-tasksadmin)
- [八、语言切换（中/英）](#八语言切换中英)
- [九、常见操作流程速查](#九常见操作流程速查)

---

## 一、整体界面长什么样

访问 `http://127.0.0.1:46001` 进入启动器。核心界面有两块：

- **启动器控制台**（首页）：启动/停止、打开 DSH、配置、实例管理。
- **工作台**（点「打开DSH」进入）：左右分栏，左边文件管理/我的任务，右边就是 DSH 对话界面。

```
┌──────────────────────────────────────────────────────────────────────────┐
│ TakeTopDSH Team 启动器                                 admin (admin) 语言 │  ← 顶部：当前账号 + 语言
├──────────────────────────────────────────────────────────────────────────┤
│ 启动器卡: 端口[46000] [▶启动] [■停止] [打开DSH]        ● Running (PID xxxx) │
│ 配置卡:   appsettings URL / 工作区路径 / 缺省语言            [保存]         │
│ 实例管理卡: 用户名 端口 工作区 状态 操作                                    │
│   jackzhong  46002  ...   Stopped   [Start][Delete][Reset Password]        │
│   ericliu    46003  ...   Running   [Open][Stop][Delete][Reset Password]   │
│   sofiali    46004  ...   Stopped   [Start]...                             │
│   bookge     46005  ...   Stopped   [Start]...                             │
└──────────────────────────────────────────────────────────────────────────┘
```

---

## 二、登录

1. 打开 `http://127.0.0.1:46001`，进入登录页。
2. 选择语言（右上角），输入**用户名 / 密码**，点【登录】。
   - 登录页默认语言 = 系统默认语言，可切换。
3. 登录后右上角显示当前用户名（admin 显示 `admin (admin)`）。

> 账号由管理员在「实例管理」创建 / 「Reset Password」重置。登录密码以管理员设置的为准。

**默认账号（初始密码，请尽快在控制台修改）：**

| 用户名 | 默认密码 | 说明 |
|--------|----------|------|
| admin | 12345678 | 管理员（启动器控制台 / 任务分配） |
| jackzhong | jack123 | 普通用户，实例 46002 |
| ericliu | eric123 | 普通用户，实例 46003 |
| sofiali | （由 admin 重置） | 实例 46004 |
| bookge | （由 admin 重置） | 实例 46005 |

> 提示：**admin 默认登录密码为 12345678**，可在控制台「实例管理 → Reset Password」重置其它账号。

---

## 三、启动器控制台（admin 视角）

### 3.1 启动器卡（admin 默认 DSH）

```
端口 [46000]   [▶ 启动]   [■ 停止]   [打开DSH]
              ● Running (PID 11384)
```

- **端口**：admin 默认实例端口（默认 46000）。
- **▶ 启动 / ■ 停止**：启动/停止 admin 默认 DSH。点击后按钮会显示**转圈等待**，直至完成。
- **打开DSH**：带 token 自动进入 admin 的工作台（左文件管理/我的任务 + 右 DSH）。**不要**直接访问 `127.0.0.1:46000`（会 404）。

### 3.2 配置卡

- **appsettings URL**、**工作区路径**、**外部访问地址**、**缺省语言**：修改后点【保存】。
- 「缺省语言」= 登录页/新用户首次看到的语言（改后保存即可，界面立即反映）。
> ⚠️ **重要**：首次使用请先在「配置卡」设置 **工作区路径（Workspace path）**——它是 AI 读写文件、新建实例工作区的根目录（如 E:\\WorkBuddy）。留空则用 dsh 默认目录。修改后点【保存】，新实例工作区会自动按「工作区路径/用户名」生成。

### 3.3 实例管理卡（多用户）

```
用户名[userA] 工作区[留空=自动 全局工作区/用户名] 密码[****]  [新建实例]
┌────────────┬───────┬────────────────────┬──────────┬──────────────────────────┐
│ 用户名      │ 端口   │ 工作区              │ 状态      │ 操作                      │
├────────────┼───────┼────────────────────┼──────────┼──────────────────────────┤
│ jackzhong  │ 46002 │ E:\WorkBuddy\jackzhong│ Stopped  │ [Start] [Delete][Reset]  │
│ ericliu    │ 46003 │ E:\WorkBuddy\ericliu  │ Running  │ [Open][Stop][Delete][Reset]│
└────────────┴───────┴────────────────────┴──────────┴──────────────────────────┘
```

- **新建实例**：填用户名、工作区（留空自动 `全局工作区/用户名`）、密码，点【新建实例】。
- **Start/Stop**：启动/停止该用户实例，点击有转圈等待。
- **Open**：打开该用户的工作台。
- **Delete**：删除该实例（需确认）。
- **Reset Password**：重置/创建该用户账号密码。

---

## 四、打开工作台 / 左右分栏

点任意「打开DSH / Open」后进入工作台（`/work`），为**左右分栏**：

```
┌──────────────────────┬───────────────────────────────────────────────────┐
│ ← 返回管理页        工作台 · ericliu                     [任务分配][语言]     │  ← 顶部栏
├──────────────────────┼───────────────────────────────────────────────────┤
│ [文件管理] [我的任务] │                                                   │
│  ← 左栏(可拖动/放大)  │              DSH 对话界面（右栏）                    │
│  文件管理内容         │   探索未至之境 / Into the Unknown                    │
│                      │   输入框：描述你想构建的内容…                        │
└──────────────────────┴───────────────────────────────────────────────────┘
```

- 顶栏：**返回管理页**、工作台标题（显示当前用户）、**任务分配**（admin）、**语言切换**。
- **左栏宽度**：拖动中间的竖条可调整；点顶部 **⤢** 按钮可在 30% ↔ 70% 间放大/缩小。
- **左栏两个 tab**：文件管理、我的任务。

---

## 五、文件管理（左栏）

左栏切到【文件管理】tab：

```
[上传文件] [刷新] [新建文件夹]           当前目录: /
┌─────────────────────────────────────┐
│ [+] 📁 项目文档        压缩 改名 移动 删除 │
│      📄 说明.txt        打开  压缩 改名 移动 删除 │
│ [+] 📁 图片            压缩 改名 移动 删除 │
└─────────────────────────────────────┘
```

- **上传文件**：选中本地文件上传到当前目录。
- **刷新**：重新加载列表。
- **新建文件夹**：输入名称创建文件夹。
- **文件夹**：点 `+` 展开子目录，支持多级树。
- **文件操作**：压缩 / 解压 / 改名 / 移动 / 删除。
- **预览**：点文件名，可预览的文件（图片/文本/视频）直接打开；不可预览的会提示下载。

---

## 六、我的任务（左栏）

左栏切到【我的任务】tab，列出管理员分配给**当前登录用户**的任务：

```
┌──────────────────────────────────────────────┐
│ 序号 任务类型 任务名称 状态    相关文件          │
│ 1    测试    项目计划功能测试 处理中  img_xxx.png│
└──────────────────────────────────────────────┘
```

- **任务名称**是链接：点它弹窗查看该任务的**富文本内容**（含图片，双击图片可全屏预览）。
- **相关文件**是链接：图片点击直接预览，其它类型提示下载。
- 状态：处理中（Processing）/ 完成（Done）/ 取消（Cancelled）。

---

## 七、任务分配页面 /tasks（admin）

启动器控制台右上角或工作台顶栏点【任务分配】（Tasks）进入 `/tasks`：

```
┌──────────┬────────────────────────────────────────────────────────────┐
│ 成员      │  当前成员: ericliu                        [English 语言]    │
│ Admin    │  [添加任务]                                                 │
│ jackzhong│ ┌──────────────────────────────────────────────────────┐   │
│ ericliu  │ │ 序号 任务类型 任务名称 状态    相关文件  操作            │   │
│ sofiali  │ │ 1    测试    项目计划功能测试 处理中  xxx.png [编辑][删除]  │   │
│ bookge   │ └──────────────────────────────────────────────────────┘   │
└──────────┴────────────────────────────────────────────────────────────┘
```

- 左侧**成员列表**：选一个成员，右侧显示其任务。
- **添加任务 / 编辑**：打开表单：
  - 任务类型（Type）、任务名称（Name）
  - **任务内容**：富文本，可加粗/列表/引用，**可粘贴或插入图片**（图片存到 `TaskData/Doc`），双击内容图片全屏预览
  - **状态**（Status）：处理中 / 完成 / 取消
  - **相关文件**：可多选上传，文件存到该成员工作区 `TaskData/Doc`
- 任务以 **XML** 存于该成员工作区 `TaskData/tasks-<id>.xml`。

---

## 八、语言切换（中/英）

- 各页面右上角有语言下拉（中文 / English）。
- 切换后：启动器、工作台、文件管理、任务分配、我的任务、任务编辑弹窗的**所有界面文案**（含按钮、列头、状态、标签）都会即时切换。
- 登录页默认语言 = 系统「缺省语言」，普通用户可切换。

---

## 九、常见操作流程速查

| 我想… | 怎么做 |
|-------|--------|
| 打开 DSH 使用 | 登录 → 点【打开DSH】→ 进入工作台，右侧即 DSH 对话 |
| 上传/管理文件 | 工作台左栏【文件管理】→ 上传/新建/压缩/改名/移动 |
| 查看分配给我的任务 | 工作台左栏【我的任务】→ 点任务名称看详情、点文件打开/下载 |
| 给用户分配任务（管理员） | 顶栏【任务分配】→ 选成员 → 添加任务（含富文本+图片+文件） |
| 启动某个用户的实例 | 控制台「实例管理」→ 该行【Start】 |
| 改登录语言 | 右上角语言下拉切换到 English/中文 |
| 解决右栏提示"认证要求" | 用启动器【启动/停止】该实例重拉（实例重启会刷新 token） |

---

> 有任何操作不顺，先 **Ctrl+F5 强刷** 排除缓存；实例相关异常，先在该用户行【停止】再【启动】。
